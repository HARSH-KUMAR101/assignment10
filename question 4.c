#include<stdio.h>
#include<conio.h>
int natural_num(int);
int main()
{
   int num,i,x;
      printf("enter number: ");
      scanf("%d",&num);
        for(i=1;i<=num;i++){
            x=natural_num(i);
            printf("%d\n",x);
        }
 return 0;
}
int natural_num(num_1)
{
    return (num_1);
}
